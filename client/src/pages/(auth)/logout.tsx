import Page from '@client/components/Page';
import styles from './logout.module.scss';

export default function LogoutPage() {
	return (
		<Page className={styles.LogoutPage}>
			<h1>Logout</h1>
		</Page>
	);
}
